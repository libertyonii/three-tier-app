Step 4 — The most reliable fix — update your workflow
The appleboy/scp-action can be finicky. Replace it in your ci-cd.yml with a plain SSH command that copies the file directly. Open .github/workflows/ci-cd.yml and replace the entire Copy docker-compose.yml to VM step and Deploy via SSH step with this single combined step: